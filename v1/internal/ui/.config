---
"*":
  GET:
    "*":
      headers:
        response:
            X-Consul-Index: ${ parseInt((location.search.index || 1)) + 1}
    index:
      "*":
        latency: ${ location.search.index ? fake.random.number({min: 500, max: env('CONSUL_LATENCY', 10000)}) : env('CONSUL_LATENCY', 0) }
