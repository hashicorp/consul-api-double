[
  ${
    range(10).map(
        function(item, i) {
            return range(fake.random.number({max: 10})).reduce(
                function(prev, item, i, arr) {
                    return `${prev}/${fake.lorem.word()}-${i}` 
                },
                `"${fake.lorem.word()}-${i}"`
            );
        }
    )
  }
]
