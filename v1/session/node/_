[
  ${
    range(env('CONSUL_SESSION_COUNT', 5)).map(
        function(item, i) {
            // TTL = "" | 10:84600
            return `{
                "ID":"${fake.random.uuid()}",
                "Name":"${fake.hacker.noun()}",
                "Node":"${location.pathname.get(3)}",
                "Checks":["serfHealth"],
                "LockDelay":15000000000,
                "Behavior":"${fake.helpers.randomize(['release', 'delete'])}",
                "TTL":"",
                "CreateIndex":2782,
                "ModifyIndex":2782
            }`;
        }
    )
  }
]
